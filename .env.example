# Krishi Route - Environment Variables Template

# --- Backend ---
PORT=5000
NODE_ENV=production
MONGODB_URI=your_mongodb_connection_string

# External API Keys (Required for real data)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key
AGMARKNET_API_KEY=your_agmarknet_api_key

# Optional: Use Mock Data for demonstration
USE_MOCK_DATA=true

# --- Frontend ---
# In production, the API is prefixed with /api (as configured in vercel.json)
# If deploying elsewhere, set this to your backend URL:
REACT_APP_API_URL=/api
